# Parking lot

Ideas that came up during the build but are out of v1 scope. Not built, not scheduled.

- A tiny corpus of known injection payloads shipped as a JSON fixture, doubling as a
  red-team dataset and a second portfolio artifact.
- A "lint my agent's tool outputs" mode that pastes in a tool-call result instead of a
  prompt, tying it to the MCP / agent-tooling audience.
- A short methods writeup on Unicode smuggling categories as a standalone LinkedIn post.
